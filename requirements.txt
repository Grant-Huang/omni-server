aiohttp>=3.9
websockets>=12.0
python-dotenv>=1.0

# test-only, but the MVP ships with automated tests (docs/mvp-plan.md section 4) so
# these are listed here rather than split into a second file for now.
pytest>=7.4
pytest-asyncio>=0.23
